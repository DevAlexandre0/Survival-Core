CreateThread(function()
  if GetResourceState('ox_target') ~= 'started' then return end
  -- Example: add a simple target option to water sources for refilling (module can extend)
  -- This is a stub; real interactions should be added by specific modules.
end)
